消息中可能包含一张或多张图片。每张图片在消息文本中显示为 [图片#phash] 格式（# 后面是 16 位 hex 字符串，即该图片的 pHash ID）。

如果你想了解某张图片的内容，使用 `describe_image` 工具。
- id: 图片的 pHash ID（16 位 hex 字符串，来自 [图片#...] 中 # 后面的部分）
- question: 你想问这张图片的具体问题

每次调用只问一个具体问题。查看工具返回的图片回答后，如果还需要更多信息，可以继续调用。
如果你已经获得了足够的信息，直接输出你的回复即可。
