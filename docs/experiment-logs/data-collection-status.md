# 数据采集状态

**生成时间**: 2026-05-22T07:10
**关联**: research-plan Phase 1

---

## 监听器状态

- **进程**: ✅ 运行中（`scripts/status-listen.sh`）
- **PID**: 1382806
- **运行时长**: 4 小时 40 分
- **连接状态**: 运行期间正常接收消息，后续经历断线重连（自动指数退避）
- **心跳日志**: 待下次启动生效（已加入 `src/listen.ts`）

## 数据概况

| 会话 | 消息数 | 发送者 | 时间跨度 | 类型分布 |
|------|--------|--------|----------|----------|
| `group_313214094` | 3 | 1（admin） | ~14 秒 | mixed / image / text |
| `group_1027996396` | 1 | 1（owner） | 0 | text |

**总计**: 4 条消息，2 个群聊，2 个去重发送者。

## 管线验证状态

| 环节 | 状态 | 备注 |
|------|------|------|
| 网关连接 | ✅ | `ws://10.0.0.201:6097` 连接成功 |
| 消息接收 | ✅ | 写入 `data/raw/*.jsonl` |
| JSON 格式 | ✅ | 格式合规 |
| analyze-raw | ✅ | 正常产出汇总报告 |
| 模拟消息测试 | ✅ | 20 条模拟消息全通过（详见 `pipeline-e2e-test.md`） |

## 注意事项

1. 当前数据量极小（4 条），**不足以支撑任何有统计意义的分析**。仍需持续积累。
2. 群聊活跃时间可能集中在特定时段，夜间接收量少是正常现象。
3. 监听器日志显示网关存在偶发断开，自动重连正常运作。
4. 心跳日志功能已加但尚未生效（下次启动后才输出到 stderr）。

## 下一步

- 保持监听器运行，等待数据自然积累
- 数据量达到 ~500 条后，运行 `analyze-raw` 做一次有意义的节奏分析
- 条件具备时进入 Phase 2 单变量实验
