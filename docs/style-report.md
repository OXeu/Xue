# 风格分析报告

**样本量**: 83 封有效回信
**生成时间**: 2026-05-22T06:56:55.950Z

---

## 1. 句长分布

| 指标 | Rin 当前值 | 真人群聊参考 |
|------|-----------|-------------|
| 平均句长 | 88.3 字 | 10~25 字 |
| 中位句长 | 59 字 | 8~18 字 |
| P25 | 36 字 | — |
| P75 | 103 字 | — |
| 短句比例（≤10 字） | 3.8% | 30~50%（群聊中短句占比高） |
| 长句比例（≥50 字） | 59.0% | ≤5%（真人很少写长句） |

**解读**：Rin 的平均句长 88.3 字，中位 59 字，在真人群聊范围内偏高。短句比例 3.8%（真人群聊通常 30~50%），长句比例 59.0%（真人通常 ≤5%），说明 Rin 倾向于写更完整、更长的句子，少了群聊中常见的碎片化表达。

## 2. 开头模式

| 前 15 常见开头 | 出现次数 |
|---------------|---------|
| `mock respo` | 9 |
| `让主 agent 再` | 5 |
| `改好了。` | 2 |
| `补一条专项回归测试，` | 1 |
| `继续补端到端回归：在` | 1 |
| `继续把这条黑盒回归补` | 1 |
| `先别汇报了，继续把这` | 1 |
| `继续修这条黑盒回归的` | 1 |
| `继续定位并清掉这条黑` | 1 |
| `雨，

因为你这次是` | 1 |
| `检查你刚才对来信的处` | 1 |
| `基于你刚总结的方向，` | 1 |
| `让主 agent 先` | 1 |
| `检查刚才这条最终总结` | 1 |
| `再自查一次刚才那句最` | 1 |

**解读**：Rin 最常见的开头是直接切入话题或使用确认性短语。缺乏真人群聊中常见的：表情开头、反问开头、接话头（"话说/对了/那个"）、语气词开头（"哎/哟/哈"）。开头多样性不足是 bot 风格的信号之一。

## 3. 结尾模式

| 前 15 常见结尾 | 出现次数 |
|---------------|---------|
| ` exhausted` | 9 |
| `main` 已同步。` | 7 |
| `ary block。` | 3 |
| `后把现有测试迁过去。` | 2 |
| `、逐步迁移的顺序做。` | 2 |
| `重置、逐步迁移来做。` | 2 |
| `，怎么最小侵入地改。` | 2 |
| `最关键的结构性改造。` | 2 |
| `处理压缩与动态模板。` | 2 |
| `tor”也一并卡住。` | 1 |
| `回头补这条黑盒断言。` | 1 |
| `而不是继续凭结构猜。` | 1 |
| `组回归里也稳定通过。` | 1 |
| `、或模块级缓存状态。` | 1 |
| `() calls`。` | 1 |

**解读**：Rin 的结尾方式以直接结束为主，较少出现真人群聊中常见的开放式结尾（"……"[话题自然消失]、表情收尾、反问收尾）。列表中的常见结尾很多是句中的惯用搭配被截断了。

## 4. 标点习惯

| 指标 | Rin 当前值 | 真人群聊参考 |
|------|-----------|-------------|
| 感叹号（每回信） | 0.0 | 0.5~3（群聊情感表达密集） |
| 问号（每回信） | 0.1 | 0.3~2 |
| 省略号（每回信） | 0.0 | 0.5~3（群聊省略号使用频繁） |
| 分点列表行占比 | 28.5% | ≤1%（真人极少列点） |

**解读**：Rin 的感叹号和问号使用频率是否合理需结合内容判断。分点列表行占比 28.5%——这是最明显的 bot 信号之一，真人群聊几乎不会出现结构化列表（除非在发通知），而 Rin 的中长回复中常用数字序号或破折号分点。

## 5. 语气词/连接词

| 指标 | Rin 当前值 | 真人群聊参考 |
|------|-----------|-------------|
| 语气词密度（‰） | 0.0 | 15~40‰（群聊语气词密度很高） |
| 连接词密度（‰） | 1.9 | 5~15‰ |

**解读**：语气词密度偏低说明 Rin 少了群聊中丰富的情绪表达（"呢啊吧嘛哈"）。连接词密度偏高则说明 Rin 的回复逻辑链更完整——真人在群聊中经常跳逻辑、省略连接词。这两项结合起来→Rin 的回复太"完整"了。

## 6. Markdown / 格式化

| 指标 | Rin 当前值 | 真人群聊参考 |
|------|-----------|-------------|
| 代码块出现率 | 3.6% | 极低（纯聊天场景） |
| 加粗出现率 | 8.4% | 极低 |
| 列表标记出现率 | 0.0% | ≤1% |

**解读**：代码块和加粗在纯群聊场景几乎没有。但考虑到 Rin 的回复中包含大量任务说明和代码相关话题，代码块的存在有其合理性。不过**格式过度的回复**（代码块+加粗+列表）在纯聊天群中会显得很奇怪。

## 7. 人称 / 指代

| 指标 | Rin 当前值（‰） | 说明 |
|------|----------------|------|
| "我" | 0.0 | 第一人称，体现参与感 |
| "你" | 0.0 | 第二人称，互动指向 |
| "我们" | 0.1 | 群体归属 |
| "大家" | 0.0 | 群体范围 |

**解读**："我/你"的使用比例反映对话的参与感。Rin 的回复通常是"我做了什么/我发现了什么"，"你"的使用则是指向对方。真人群聊中"我"的使用频率通常较高（表达自我观点），但"大家"使用较少（除非@全体）。

---

## 综合差距总结

| 维度 | 差距等级 | 核心问题 |
|------|---------|---------|
| 句长 | 🔴 明显 | 句子过长，短句比例不足，碎片化表达少 |
| 开头 | 🟡 中等 | 开头方式单一，缺少语气词/表情开头 |
| 结尾 | 🟡 中等 | 结尾太"完整"，缺少话题自然消散 |
| 标点 | 🔴 明显 | 分点列表是最大 bot 信号 |
| 语气词 | 🔴 明显 | 语气词密度偏低，缺少情绪颗粒度 |
| 格式化 | 🟢 可接受 | 代码块在技术群合理，但需控制频率 |
| 人称 | 🟢 可接受 | 分布基本合理 |

**标记**: 🔴 明显差距  🟡 有差距但影响中等  🟢 可接受
